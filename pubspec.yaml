name: privacy_lookup
description: Privacy info lookup app by eri
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.3.0
  cached_network_image: ^3.3.1
  advanced_root_detection: ^0.0.5
  connectivity_plus: ^6.1.0
  cryptography: ^2.7.0
  crypto: ^3.0.5
  device_info_plus: ^11.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true